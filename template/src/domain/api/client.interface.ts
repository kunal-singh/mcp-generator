/**
 * Interface for the {{Name}} API client
 * Define all the methods your client needs to expose here.
 * Both the concrete client and the cached wrapper implement this interface.
 */
export interface I{{Name}}Client {
  // TODO: Replace with your actual API methods
  // Example:
  getData(id?: string): Promise<unknown>;
}
